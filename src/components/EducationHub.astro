---
import { getI18N } from "@/i18n";
import CardDocuments from "../components/CardDocuments.astro";
import NoDataCard from "../components/NoDataCard.astro";
import { cardsData } from "../data/educationhub.js";
// const cardsData: any[] = [];
//Para cambiar idioma de textos
const { currentLocale } = Astro;
const i18n = getI18N({ currentLocale });
---

<div class="flex flex-col gap-y-3">
  <div class="text-center mb-6">
    <h2 class="text-2xl font-bold text-hub-title dark:text-hub-title-dark">
      {i18n.TRAINING_SECTION.TITLE}
    </h2>
  </div>
  
  {
    cardsData.length > 0 ? (
      cardsData.map((card) => (
        <CardDocuments
          id={card.id}
          url={card.url}
          href={card.href}
          title={card.title}
          subtitle={card.subtitle}
          imageSrc={card.imageSrc}
          imageAlt={card.imageAlt}
        />
      ))
    ) : (
      <NoDataCard
        message={i18n.TRAINING.empty.message}
        subtitle={i18n.TRAINING.empty.message2}
        imageSrc="/img/notion/white/my-notion-face-transparent_8.png"
      />
    )
  }
</div>
