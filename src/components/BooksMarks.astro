---
import { getI18N } from "@/i18n";
import CardDocuments from "../components/CardDocuments.astro";
import NoDataCard from "../components/NoDataCard.astro";
import { cardsData } from "../data/bookmarks.js";

// const cardsData: any[] = [];
//Para cambiar idioma de textos
const { currentLocale } = Astro;
const i18n = getI18N({ currentLocale });
---

<div class="flex flex-col gap-y-3">
  <div class="text-center mb-6">
    <h2 class="text-2xl font-bold text-hub-title dark:text-hub-title-dark">
      {i18n.PRICING_SECTION.TITLE}
    </h2>
  </div>
  
  {
    cardsData.length > 0 ? (
      cardsData.map((card) => (
        <CardDocuments
          id={card.id}
          url={card.url}
          href={card.href}
          title={card.title}
          subtitle={card.subtitle}
          imageSrc={card.imageSrc}
          imageAlt={card.imageAlt}
        />
      ))
    ) : (
      <NoDataCard
        message={i18n.PRICING.empty.message}
        subtitle={i18n.PRICING.empty.message2}
        imageSrc="/img/notion/white/my-notion-face-transparent_9.png"
      />
    )
  }
</div>
